#!/bin/bash

[ $EUID -eq 0 ] || exec sudo "$0" "$@"
cd "$(dirname "$(readlink -f "$0")")"
squidGuard -C all
squid -k reconfigure
rm -Rf ../bypass/timed/*
rm -Rf ../unblock/timed/*
touch ../lockdown
reboot


